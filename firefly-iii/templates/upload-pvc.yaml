# Generated by HelmRenderer.py

{{ if .Values.persistance.upload.create }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "firefly-iii.uploadPVCName" . }}
  labels:
{{ include "firefly-iii.labels" . | nindent 4 }}
spec:
  accessModes: {{ .Values.persistance.upload.accessModes }}
  resources:
    requests:
      storage: {{ .Values.persistance.upload.size }}

{{ end }}
